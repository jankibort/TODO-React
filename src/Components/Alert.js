import React from 'react';

const Alert = () => {
    console.log(props.success)
    // let classes = "alert alert-";
    // classes += success ? "success" : "danger";
    // let content = success ? "Task successfully added!" : "Error: All fields are required!";

    // return (
    //     <div class={classes} role="alert">
    //         {content}
    //     </div>
    // );
}

export default Alert;